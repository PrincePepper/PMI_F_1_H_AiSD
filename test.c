#include <stdio.h>
#include <string.h>

#define MAX_ELEMENT 1000
char mass[MAX_ELEMENT];

int main() {
    for(int i=0;i<MAX_ELEMENT)
    scanf("%s",&mass[i])
    FILE *fin = fopen("input.txt", "r");

    fscanf(fin,"%s",)

}